#User input

#Tells python that we want input from user
#User gets prompt first and then input is stored in name variable
#User has to click enter
#can have multiple inputs back to back that executes after each input
name = input("Enter your name: ")

age = input("Enter your age: ")

print("Hello " + name + "!")
print("You are " + age)

